# LeetCode 学習リポジトリ

## 構成
- `problems/` — 問題ごとのフォルダ（番号_問題名）
- `templates/` — Python/C++ テンプレート
- `run.sh` — ローカル実行スクリプト

## ルール
- 解答は `problems/{番号}_{問題名}/` に配置
- 各問題フォルダに README.md（考察・計算量・反省）を含める
- ルートの README.md に進捗テーブルを更新する
- `/solve [問題番号 or URL]` で新しい問題を開始
